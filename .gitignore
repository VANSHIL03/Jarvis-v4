# Python & Environment
__pycache__/
*.py[cod]
*$py.class
.env
.venv/
env/
venv/
ENV/

# Temporary audio files & recordings
*.mp3
*.wav

# Logs & database
logs/
*.log
*.db
data/
scratch/

# Ad-hoc test scripts dropped in the repo root. Anchored with a leading slash so
# it stops swallowing the real suite in tests/ -- an unanchored test_*.py matched
# at every depth, which silently excluded every file in tests/ from version
# control.
/test_*.py
